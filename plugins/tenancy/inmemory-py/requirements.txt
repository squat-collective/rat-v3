# Runtime deps for rat-tenancy-inmemory-py. Pinned to the versions the
# cross-run was verified against (python:3.12 container). protobuf must be >= the
# gencode's runtime pin (see rat/*/v1/*_pb2.py ValidateProtobufRuntimeVersion).
grpcio==1.80.0
protobuf==7.35.0
