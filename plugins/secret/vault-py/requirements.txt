# Runtime deps for rat-secret-vault-py. Vault is driven with stdlib urllib (KV v2 HTTP
# API) — deliberately no vault client library: one fewer supply-chain edge on the
# platform's most sensitive plugin.
grpcio==1.80.0
protobuf==7.35.0
