# Runtime deps for rat-engine-duckdb-ml-py (data-dev plane engine + ML).
# Extends duckdb-py's deps with numpy — REQUIRED for list-returning UDFs (the embed()
# UDF returns FLOAT[]; DuckDB's create_function needs numpy to marshal list results).
# vss / ducklake / httpfs are DuckDB extensions installed at runtime (INSTALL ...), not
# pip deps. minilm (sentence-transformers) and ollama are operator opt-ins, not pinned
# here — the default embed backend 'hash-256' is pure stdlib.
grpcio==1.80.0
protobuf==7.35.0
duckdb==1.5.3
pyarrow==24.0.0
numpy==2.2.6
