# rat-strategy-incremental-embed-py — the data-dev plane ELT strategy.
# EXPLORATORY (experiments/data-dev-plane). Additive; the frozen strategy/v1 surface is
# unchanged. A real incremental embedding ELT (watermark→merge→embed-only-new→flush→
# snapshot) — composes capabilities by URI, names no concrete plugin. Notably requires
# NO format capability: the engine writes the lake directly (DuckLake subsumes format).
# Validates against contracts/schema/{plugin.v1.json, kinds/strategy.v1.json}.
api_version: rat/1
kind: strategy
metadata:
  name: rat-strategy-incremental-embed
  version: 0.1.0
  authors: ["RAT data-dev plane"]
  description: "Incremental embedding ELT: watermark, merge, embed-only-new, snapshot"
  license: Apache-2.0
compatible_core: ["rat/1"]
provides:
  - capability: rat://strategy/v1/apply
requires:
  - capability: rat://catalog/v1/get-table
  - capability: rat://catalog/v1/register-table
  - capability: rat://engine/v1/execute
  - capability: rat://catalog/v1/commit-table
resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
