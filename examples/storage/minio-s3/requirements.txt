# Runtime deps for rat-storage-minio-s3.
# boto3 is needed only by the REAL MinIO STS path (creds.MinioSTSMinter, imported
# lazily). The offline scope-receipt mode (the conformance harness) needs neither boto3
# nor a running MinIO — so `make conformance` does not install boto3.
grpcio==1.80.0
protobuf==7.35.0
boto3==1.40.0
