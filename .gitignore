# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Claude Code per-user settings (project-shared settings.json IS committed)
.claude/settings.local.json

# Local notes (not committed)
NOTES.md
SCRATCH.md
.notes/

# When code starts landing:
target/
node_modules/
dist/
build/
*.pyc
__pycache__/
.venv/
.env
.env.local

# Generated artefacts
# NOTE: the vendored SDKs under contracts/sdks/<lang>/ ARE committed (ADR-006 D1).
# Do NOT ignore *.pb.go / *_pb2.py here — they are the deliverable, not scratch.
# (contracts/.gitignore handles the only proto-related exclusion: sdks/go/go.sum.)
gen/
