# Current — what's in flight right now

> **Always read this first when opening a Claude session on this project.**
> Updated: 2026-06-03 (🎉 **PHASE 8 RE-SEALED — `rat/5.9`.** The **marketplace** (`kind: marketplace` axis) + project lifecycle: discovery across **local** stamped-manifest images + **added** index files/**remote URLs** — `rat search` (matches name/kind/desc **and capability**; `TRUST` column), `rat list`, **`rat add`** / **`rat remove`** (symmetric: add appends a `[[plugin]]` block, remove strips it preserving comments/siblings + cleans the managed manifest), `rat marketplace add/list/keygen/sign/verify` (config at `~/.config/rat/marketplaces.json`; built-in `official` shorthand), **`rat add` auto-suggest** + **`rat add --with-deps`** (transitively pulls every provider until the plane resolves; manifest synthesized from each entry, no pull at declare-time). **Remote indexes**: bounded timeout, offline cache, surfaced warnings. **Signed provenance**: detached ed25519 signatures pinned per source (`--pubkey`); tampered index = hard error; `--with-deps --require-signed`. Reference index `marketplace/official.json` + README. Ecosystem story complete: **author** (`rat plugin …`) · **run** (`rat serve`/`add`/`remove`) · **distribute** (GHCR) · **discover** (marketplace, local+remote, signed). Sealed line: `rat/2.0` core · `rat/2.5` platform+daemon UX · `rat/3.0` multi-surface UI · `rat/3.5` distribution · `rat/4.0` authoring · `rat/4.5` authoring↔runtime · `rat/5.0` dependency resolution · `rat/5.5`–`5.9` marketplace (discover · auto-resolve · remote index · signed entries · `rat remove`). 🎯 **NEXT (open, pick by appetite):** `rat add`/`remove` *materializing* into a live daemon (RegisterPlugin/DeregisterPlugin RPC, ADR-023 — today declarative, `rat up` applies); publish + sign `official.json` on the `rat-dev` Pages site (URL + key are placeholders); remaining ADR-026 (launch-time manifest, conformance in `test`, build-backend/template axes); ADR-025 surface follow-ons. Branching: `main` is the sealed line (`rat/5.9`); start the next `phase-N` for new work.)

## Status one-liner

**Phase 0 — 🎉 SEALED (`rat/1.5`).** **Phase 1 (core) — `rat/2.0`.** **Phase 2 (platform + daemon UX) — `rat/2.5`.** **Phase 3 (surfaces & consumers) — `rat/3.0`.** **Phase 4 (distribution: GHCR release) — `rat/3.5`.** **Phase 5 (plugin authoring & packaging) — `rat/4.0`.** **Phase 6 (authoring↔runtime: manifest-from-image) — `rat/4.5`.** **Phase 7 (dependency resolution) — 🎉 SEALED (`rat/5.0`).** **Phase 8 (the marketplace) — 🎉 SEALED (`rat/5.9`):** discovery (`rat search`/`list`/`marketplace`) over local stamped images + added index files + remote/HTTP indexes (timeout + offline cache), ed25519-signed provenance (pinned per source, `--require-signed`), `rat add` auto-suggesting + `rat add --with-deps` transitively auto-resolving, and `rat remove` as the symmetric inverse. Everything below `rat/2.0` is the frozen wire; Phases 2–7 added only reference plugins + the core daemon + the platform + the UX + the multi-surface UI + the release pipeline + the authoring toolkit + the manifest-from-image handoff + dependency resolution on top (additive — `make breaking` clean throughout, no proto/axis change since `rat/2.0`). The running platform proves the spine end to end; ADRs 019–026 carry the thinking. **Next (open, pick by appetite): `rat add` auto-suggest (marketplace index); remaining ADR-026 (launch-time manifest, conformance in `test`, signing); ADR-025 surface follow-ons.** *(Detailed Phase-0/1 history is preserved below as the historical record.)*

## ✅ Commitment gate — CLEARED (ADR-015)

ADR-013 deferred the gate to the spike's report; [reviews/10](../reviews/10-phase-1-spike-exit.md) delivered it (frozen wire held), and Tom made the call: **commit to the full core build.** Scope: this clears **Phase-0 → Phase-1**. The later user-pull gates remain hard — phases.md **Gate B** (≥10 real solo users), **Gate C/D**, and **Q02** (external peer review, still owed — schedule *during* the build). Rationale (Q01, why v3 over v2) recorded in ADR-015 from the founding premise; refine there if your conviction is framed differently.

## 🎉 Phase 1 — COMPLETE & SEALED (`rat/2.0`)

The six things + cross-cutting enforcement ([ADR-001](../docs/architecture/adrs/001-everything-is-a-plugin.md)) are built and green against **real launched plugins**: `core/{manifest, registry (+ NewVerified), gateway, supervisor, deploymentruntime (local-process + podman), lease, reconciler, conformance, arrowticket, composition}`. All 9 board exit criteria below are met; `phase-1` is merged to `main` and tagged `rat/2.0`.

**Definition of done** (the board's exit criteria — [reviews/10](../reviews/10-phase-1-spike-exit.md)): **✅ C5** (extended to real providers — DONE) · **✅ C4** terminal audit incl. denials (DONE) · **✅ C3** (no-deadline idle-timeout backstop — DONE) · **✅ D1** a real *enforcing* deployment-runtime (podman, full I9 profile — DONE) · **✅ D2** (wire into the real bulk leg — DONE) · **✅ D3** storage-cred isolation (DONE) · **✅ D4** conformance-attestation *enforced* (DONE) · **✅ C1** (against real backends — DONE) · **✅ sre#4** reconciler crash-loop backoff/jitter + lease-thrash guard (DONE). **🎉 ALL 9 MET.**

**Immediate next concrete step:** 🎉 **Phase 1 is SEALED — `rat/2.0`** (merge `6b85477` carried sre#4, the last of the 9; this commit records the seal). All board exit criteria met, all green against real launched plugins, frozen wire intact. **Now owed / next:**
- 🛰️ **ACTIVE EXPLORATORY TRACK — the "data-dev plane" experiment** ([`experiments/data-dev-plane/README.md`](../experiments/data-dev-plane/README.md), branch `phase-1-data-dev-plane`). Tom is a **solo dev**, so the Q02 *external human* review below is impractical → we validate the platform **practically** instead, by composing a real, scalable, **end-to-end ML lakehouse** workflow from plugins (principle #8). Design doc DONE. ✅ **Build steps 2 + 3 DONE (2026-06-02).** **Step 2** — the DuckDB heart runs **local end-to-end over gRPC**: [`duckdb-ml-py`](../examples/engine/duckdb-ml-py/) engine (+ `embed()`/`vss`/`ducklake`, a new embed golden) and [`ducklake-py`](../examples/catalog/ducklake-py/) catalog, via [`run-local.py`](../experiments/data-dev-plane/run-local.py) / `make data-dev-local`. **Step 3** — the pipeline goes **REMOTE**: [`minio-s3`](../examples/storage/minio-s3/) storage plugin vends short-TTL tenant-scoped **STS** creds (first 5c read/write split impl); data on **S3/MinIO**, DuckLake metadata on **Postgres**; [`run-remote.py`](../experiments/data-dev-plane/run-remote.py) / `make data-dev-remote` runs the same flow distributed with **search distances byte-identical to local** (data plane unchanged when storage goes remote), Parquet on S3, D3 cross-tenant isolation enforced. **Additive: conformance 34/34, `make breaking` clean, no proto/axis.** **Step 4** — a real **incremental-embed ELT** [strategy](../examples/strategy/incremental-embed-py/) (composes capabilities through the invoke gateway, names no plugin; requires no `format` cap — the engine writes the lake): [`run-strategy.py`](../experiments/data-dev-plane/run-strategy.py) / `make data-dev-strategy` proves run 1 embeds 12 (full), run 2 embeds 3 (delta — incrementality), replay embeds 0 (idempotent C1). **Step 6** — a **VS Code extension** ([`vscode-rat`](../examples/ui/vscode-rat/)) as a UI client: DuckLake catalog tree, run-pipeline, SQL grid, 🔍 semantic search, plugin health; compiles clean (strict `tsc`), talks to a Python **gateway BFF** (`make data-dev-gateway`) that owns the in-proc stack and re-exposes it as JSON (finding F9 — the reference engine's Arrow leg is in-proc, so an external UI needs a data-leg helper; the connectionless Connect TS SDK is the production control-plane path). 🎉 **The experiment is now END-TO-END: storage → catalog → engine+ML → strategy → UI, local + remote, 5 new plugins + a gateway, zero frozen-wire change.** Findings F1–F9 captured (README §10).

## 🎯 NEXT (active) — the data platform bundle ([ADR-020](../docs/architecture/adrs/020-data-platform-bundle.md), **Accepted**, Phase 2)

**The product: v2 rebuilt on v3.** `ratatouille-v2` (landing zones → medallion → quality-gated, **scheduled** refreshes) rebuilt with every responsibility **decoupled into a v3 plugin** behind the gateway, **DuckLake as the catalog** (replacing Nessie/Iceberg), and **VS Code + `ratctl`** replacing the portal. It is **always-on + self-driving**: `rat serve` runs 24/7 and a **scheduler plugin** fires hourly refreshes on its own; state lives remote (DuckLake-on-Postgres + data-on-S3). The **v2→v3 mapping** (ADR-020) is the spine: `ratd`→`rat serve`; scheduler→**scheduler plugin**; runner→**engine + pipeline strategy**; ratq→engine query; portal→**vscode-rat + ratctl**; postgres→**state-backend plugin**; minio→**storage plugin**; **nessie→DuckLake catalog**. *Already exist:* engine, DuckLake catalog, storage, vscode-rat, ratctl, rat serve. *New:* scheduler plugin, state-backend plugin, the pipeline/medallion strategy (merge strategies + quality gate), attach mode.

**Build order (each provable):** **✅ S1 — DONE** the decoupled stack runs the medallion through `rat serve`, remote (engine + DuckLake catalog + Postgres + MinIO; attach mode, no DinD). → **✅ S2 — DONE** self-driving: a pipeline `strategy` plugin (the medallion as `strategy.apply`) + a `scheduler-backend` driver that fires it on a cron — the stack refreshes on its own, audited. → **🟡 S3 — quality gates DONE** (tests block the commit, proven pass+fail); merge-strategies (incremental) + read-isolation (DuckLake branching) remain. → **🟡 S4 — state-backend DONE** (run history); **S4b — UI control-path DONE** (`platform/bff.py` fronts the real gateway: run history + trigger, proven via curl, audited). The VS Code extension *UI itself* (a platform UX pointed at the BFF, run interactively) + the F9 bulk data-leg remain.

**✅ S1 built + proven (2026-06-02, branch `phase-2-platform-bundle`).** `platform/compose.yaml` (the always-on stack) + attach-mode `platform/plane.yaml` + env-driven `platform/run.py` + `make platform-{up,run,down}` + small additive engine/catalog entrypoint tweaks (S3 secret from env; remote extensions). `make platform-up` brings up Postgres + MinIO + engine + catalog + `rat serve` (attaching to the plugins by service name); `make platform-run` runs the medallion through the **real gateway** — bronze 9 → silver 4 → gold 2 (revenue 59.98 / 179.49), **6 audited control hops** (4× engine.execute + catalog register/commit), the gold snapshot committed to the **DuckLake catalog**, Parquet for all 3 layers on **S3/MinIO**, metadata on **Postgres**. *v2's pipeline, on v3 plugins, DuckLake catalog.* `make core-test` + `breaking` green; proto/axis untouched.

**✅ S2 built + proven (2026-06-02, branch `phase-2-scheduler`).** `examples/strategy/sql-pipeline-py` (the medallion as `strategy.apply`, dialing the gateway for its engine/catalog hops) + `examples/scheduler/cron-py` (a `scheduler-backend` driver firing `strategy.apply` on an interval) + `minio-setup` (bucket provisioning) + the platform plane/compose wiring + `run.py` simplified to one `strategy.apply`. `make platform-up` → the scheduler self-drives: tick 1/2/3 → snap-4/8/12, fresh gold Parquet each, every fire audited as `rat-scheduler`. The audited chain is v2's `ratd → runner → engine`, per-hop C5-enforced.

**✅ S3 quality gates built + proven (2026-06-02, branch `phase-2-quality`).** The pipeline strategy runs `project/tests/*.sql` after building the layers; a violation raises `FAILED_PRECONDITION` *before* the catalog commit (so the published snapshot doesn't advance). The F9 dodge: each test is a `CREATE … AS <test>` so `rows_affected` is the violation count (no Arrow row-pull). Proven: passes commit; an injected failing test gated the next self-driving tick (no commit).

**✅ S4 state-backend built + proven (2026-06-02, branch `phase-2-state`).** `examples/state/postgres-py` — a Postgres-backed `state-backend` plugin (frozen state/v1 Get/Put/List + CAS), reusing the stack's Postgres. The scheduler records a run per fire (`state/put runs/<tick>`); the runner lists the history (`state/list` + `get`). Proven: `make platform-up` self-drives + records; `make platform-run` lists `3 run(s) recorded …`; every state hop audited (`rat-scheduler → state/put`, `platform-runner → state/list/get`). The platform now has v2's run history, as a plugin behind the gateway.

**The v2→v3 rebuild's PLUMBING is standing (S1–S4):** decoupled stack · self-driving scheduled refresh · quality-gated commits · run history — all through the gateway, audited. But the *product* fell short: the pipeline was baked into the infra (hardcoded medallion, model list in a compose env, one global interval) — not the **code-driven** platform v2 was.

**🧭 DIRECTION REDIRECTED — [ADR-021](../docs/architecture/adrs/021-orchestrator-pipelines-as-code.md) (Proposed).** A fundamental rethink: **rat orchestrates *capabilities* and never knows what a "pipeline" is; your data work is a dbt project (code) you `rat apply`; the infra declares only plugins.** The pipeline *language* becomes a plugin (a **`pipeline-runner` axis** — `dbt-runner` first — so rat reinvents no DAG/`ref()`/Jinja/tests; dbt does). Plugin deps = capability composition. ADR-020's S1–S2 decoupled stack/scheduler/state/gateway stand as the *foundation*; its bespoke "model-list strategy" (S3/S4 product layer) is **replaced** by the runner axis.

**✅ ADR-021 first slice PROVEN (2026-06-02, branch `phase-2-dbt-runner`) — real dbt, orchestrated by rat.** `examples/runner/dbt-duckdb` (a **dbt-runner plugin**: on `strategy/apply` it runs `dbt build`; dbt in its own venv for protobuf isolation) + `platform/dbt-project/` (a standard dbt project — the user's code — `ref()` + native tests + one `rat.yaml`). Proven: the scheduler fires `strategy/apply` → the dbt-runner → `dbt build` runs the medallion + tests → **`PASS=7 ERROR=0`**; audited (`rat-scheduler → strategy/apply → rat-pipeline`); failed/succeeded runs recorded in state. **rat orchestrates capabilities + a schedule; the pipeline is dbt code; the language is a plugin.**

**🔌 SECOND REDIRECT — [ADR-022](../docs/architecture/adrs/022-plugins-are-launched-not-composed.md) (Proposed): plugins are LAUNCHED, not composed.** Tom's second trigger: *adding a plugin should be almost nothing.* The platform was built in **attach mode** (a compose service per plugin → the infra grows per plugin — backwards). Fix: rat **launches** plugins from one `plugins.yaml` (`name`/`image`/`needs`/`secrets`/`config`); the infra shrinks to a fixed bootstrap (rat + Postgres + MinIO + secret plugin); secrets live in a secret plugin. Deployment-runtime: **socket-mount local, k8s prod.** Surfaces a load-bearing dep (Q5): a concurrency-safe `gateway.SetProvider` re-bind (the Phase-A reconciler-rewire gap).

**Immediate next concrete step:** with [ADR-021](../docs/architecture/adrs/021-orchestrator-pipelines-as-code.md) (pipelines as dbt code) + [ADR-022](../docs/architecture/adrs/022-plugins-are-launched-not-composed.md) (launch, not compose) as the spine, the build path: **✅ (1) the `gateway.SetProvider` re-bind — DONE** (`core/gateway`: mutable provider map + `SetProvider`/`RemoveProvider`, `-race` clean; unblocks hot reconcile, launch-with-lifecycle, AND runtime registration) → **✅ (2a) reconciler re-wire hook — DONE** (`core/reconciler`: a `Rewire` Bind/Unbind hook fired on health transitions; `TestRewireOnRelaunch` proves a crashed plugin re-binds at its new endpoint — the self-healing re-wire path is complete + tested) → **✅ (2b) the launch-mode daemon — DONE** (`cmd/rat`: `launchPlane` runs the reconciler with a `gatewayRewire` adapter as the **sole launcher**, replacing `BringUp`'s static launch; `rat serve` now launches + supervises + self-heals; `make core-serve-smoke` green) → **🟡 (2c) launch mode in the platform — first step DONE** (chose the *reliable decoupled* path over a blind socket-mount): `rat serve` (host, podman runtime) launches a **separate plugin container** from `core/cmd/rat/plane.podman.yaml` (image `rat/stateplugin:dev`, `make stateplugin-image`) — proven: launch → wire → `ratctl` routes to the container (pid 1) → C5 deny → drain. *Adding a plugin = one plane entry + an image; rat launches it; no per-plugin compose service.* **✅ Python plugin images BAKED** + **✅ the slim launch-mode platform PROVEN** (`platform/plugins.yaml`): `rat serve --plane plugins.yaml` (host, podman runtime) **launches the dbt-runner as its own container** and `ratctl → strategy/apply` ran the **full medallion inside it** (`PASS=7 ERROR=0`) — no per-plugin compose service; *adding a plugin = one entry + an image.* The dbt-runner image bakes a demo project (real delivery via `rat apply` later). **✅ THE FULL LAUNCH-MODE PLATFORM PROVEN (2026-06-03, branch `phase-2-full-launch`):** `plugins.yaml` now declares **4 launched plugins** — `rat-pipeline` (dbt-runner), `rat-state` (Postgres-backed run history), `rat-scheduler` (self-driving clock), `platform-bff` (UI control-path) — + a register-only `platform-runner` driver; the infra shrank to `platform/compose.infra.yaml` = **just Postgres + MinIO**. `rat serve --plane plugins.yaml` (host, podman runtime) **launched all 4 plugin containers, wired them, and the platform self-drove**: ticks 2–10 each `refreshed` (a real `dbt build` in the dbt-runner container) **and** recorded to run-history via `rat-state → Postgres` (verified durable: 9 `success` rows in `rat_state`); the **launched bff** served `/api/runs` reading that history back through the gateway; audit confirms the launched plugins talk *only* through the gateway (`rat-scheduler → strategy/apply → rat-pipeline`, `→ state/put → rat-state`). Drain tore down every launched container. The **scheduler/bff drivers** got a trivial TCP health-port (`_serve_health` / bind `RAT_PLUGIN_ADDR`) so the reconciler can launch+supervise them. *Adding a plugin = one `plugins.yaml` entry + an image; the infra never grows.* **✅ 2c COMPLETE — SOCKET-MOUNT PROVEN (2026-06-03, branch `phase-2-socket-mount`):** rat is now **itself a container** that drives the host's rootless podman over a mounted socket (DooD) and launches each plugin as a **sibling** on a shared `rat-net`, dialing them **by name** via podman DNS (k8s pod-to-pod shape) — no host-port publishing. New podman-runtime SIBLING mode (`NewPodmanNetworked` / `$RAT_PODMAN_NETWORK`; endpoint `<name>:50051`, `--add-host` keeps `host.containers.internal` for host backends; I9 intact — `TestPodmanSiblingNetwork` proves a peer resolves+connects by name, `make core-test-podman` green); rat **injects `RAT_GATEWAY`** per topology so the SAME `plugins.yaml` runs host-mode *and* socket-mounted unchanged; the rat image now carries the podman client (`podman-remote` wrapper). **Proven end-to-end** (`make platform-socket` / [`platform/run-socket-mount.sh`](../platform/run-socket-mount.sh)): rat-as-a-container launched all 4 sibling plugins, they interconnected by name, self-drove (ticks 2–10, real `dbt build` each), recorded durable run history to host Postgres, and the bff served `/api/runs` reached **by name from a peer** (no host port) — every hop audited through the *containerized* gateway. ADR-022 Q3 (sibling networking) + Q5 (gateway re-bind) resolved; Q2 partially (RAT_GATEWAY injection). **✅ (3) SECRET PLUGIN PROVEN (2026-06-03, branch `phase-2-secret-plugin`):** a `kind: secret-backend` plugin ([`examples/secret/env-py`](../examples/secret/env-py/), env-backed, on the **frozen `secret/v1`** contract) holds the platform's secrets in **one trust boundary**; `rat-state` now resolves its Postgres DSN from `ref://state/pg-dsn` via `rat://secret/v1/resolve` **at first use** (gateway-routed, C5-authorized, audited, tenant-scoped, redacted) — so `rat-state`'s env carries **no credential**, only an opaque ref. Proven: the new audit hop `rat-state → secret/v1/resolve → rat-secret` (lazy, once, then cached); the DSN's password **never appears** in rat's audit log; the platform self-drives + records durable run history (so the DSN resolved + connected). The secret VALUE still reaches `rat-secret` via its own launch env (`$RAT_SECRETS`) — production swaps that for Vault/KMS behind the same Resolve (the `LaunchSpec` file/secret channel is ADR-022 Q4). `make breaking`/`core-test` green; additive (added `secret/v1` to the gateway's routable descriptors + `rat-state` `requires`). **✅ (5) `rat apply` PROVEN (2026-06-03, branch `phase-2-rat-apply`) — your pipeline is code you submit.** The dbt project is no longer baked into the image: `ratctl apply --project <dir> --name <name>` tar.gz's your project and ships it to the running orchestrator, stored in the **state-backend** at `projects/<name>` via `rat://state/v1/put` (same C5-authorized, audited gateway path — **no new axis, no proto change**: the state-backend *is* the project store). The dbt-runner fetches `projects/<name>` via `rat://state/v1/get` on each run (revision-cached: re-extracts only on change), and runs **your** code; the baked sample is the fallback until something is applied. **Proven live:** baked run `PASS=7`; after `ratctl apply` the runner extracted the project (rev 1) and the next run built the apply-only `applied_marker` model → `PASS=8` (DuckDB ground-truth `('applied-via-rat-apply', 42)`); re-apply bumped to rev 2 → re-extract → value propagated `42→99`. Audit shows the full path: `platform-runner → state/put → rat-state` (apply) + `rat-pipeline → state/get → rat-state` (fetch). New: `ratctl apply` (+`tarProject`/`TestTarProject`); dbt-runner fetches+extracts; `rat-pipeline` `requires state/get`, `platform-runner` `requires state/put`; rat injects `RAT_PLUGIN_NAME` (caller identity) alongside `RAT_GATEWAY`; `plugins.yaml` `RAT_PROJECT_KEY`. `make core-test`/`breaking`/`ratctl-smoke` green; additive. **Remaining in the launch-mode arc (orthogonal follow-ons):** (4) **Q2** dbt→shared-DuckLake (so the UI sees real tables), (6) ADR-022 Q4 secret bootstrap (a `LaunchSpec` secret channel so even `rat-secret` loads from a real backend, not env), (7) per-project cron + the dedicated `pipeline/v1/run` axis, + the `k8s` deployment-runtime (prod profile). *(Minor: the SIGTERM drain occasionally races and leaves plugin containers up — host-mode launch teardown robustness; cleanup is a one-liner.)* → (3) **secret plugin** → (4) **Q2 dbt→shared-DuckLake** → (5) `rat apply` + per-project cron + `pipeline/v1/run`. *(The bespoke `sql-pipeline-py` strategy + S3/S4 strategy-quality are superseded by the dbt-runner.)*

**Immediate next concrete step — the remaining slices (pick by appetite):**
- **S4b finish — the VS Code extension UI itself:** `platform/bff.py` already routes the UI's control path through the real gateway (run history + trigger, proven). What's left is a *platform-shaped* VS Code UX (medallion layers + run history) pointed at the BFF, run interactively (the existing `vscode-rat` is experiment-shaped — semantic search; the UI can't be proven headlessly). + the F9 bulk data-leg (table/row preview).
- **S3 follow-ons:** (a) **merge strategies** — incremental on `unique_key`+watermark in the pipeline strategy (the [`incremental-embed`](../examples/strategy/incremental-embed-py/) strategy shows the shape); (b) **read-isolation** — investigate DuckLake branching (snapshots/time-travel, not Nessie git-branches) for v2's branch-on-failure-discard.
- **Architectural follow-ons (when wanted):** promote the cron driver to the full scheduler-backend axis (`Schedule`/`Cancel`/`WatchDue`); a real audit-log plugin; the runtime self-registration idea (parked in [ideas/inbox.md](../ideas/inbox.md)).

---

### ✅ Phase-2 prerequisites — DONE (ADR-019)

ADR-019 delivered the runtime this bundle assembles. (Detail retained below.)

The experiment surfaced that the sealed core is built+tested but is a **library, not a server** — so the UI talks to a BFF stand-in instead of the real gateway. **ADR-019 is Accepted and written to be executed cold** (Implementation map + per-phase runbook + a fresh-session kickoff checklist). This is the **Phase 2 (Solo deployment) kickoff** — make-it-real infra, not behind Gate B.

**Build order:** **✅ Phase A — DONE** — `core/cmd/rat serve`: parse a `plane.yaml` → `supervisor.BringUp` (launches the core's Go **test plugins** via the deployment-runtime) → serve `plane.Gateway` over **TCP** (`corev1.RegisterCapabilityInvokeServiceServer` + `net.Listen`) → signal-driven drain (`GracefulStop` + `Shutdown`). *First time the core runs as a server.* → **Phase B (NEXT)** — containerize the data-dev Python plugins (image-only, no proto change) → run the pipeline through the **real** gateway (C5 + audit). → **Phase C** — `deploy/data-dev-starter/` compose stack (daemon in **attach mode** + base plugins + MinIO/Postgres) → `compose up` gives a beginner a working data-dev plane.

**✅ Phase A built + proven (2026-06-02, branch `phase-1-adr-019-rat-serve`).** `core/cmd/rat/{main,plane,auditor,descriptors}.go` + manifests + `plane.example.yaml` + `serve_test.go`. The real `rat serve` binary boots the `stateplugin` over TCP and: an authorized capability routes (C5 allow + audit line), an undeclared one is `PERMISSION_DENIED` (C5 deny + audit line), SIGTERM drains cleanly. `make core-test` + `make core-serve-smoke` + `make breaking` green; additive (no proto/axis, `rat/2.0` untouched). **Phase-A finding (→ backlog):** the reconciler crash-restart loop is **not** wired — `gateway.New`'s provider-conn map is fixed at construction (no re-bind), so a reconciler restart can't re-dial a relaunched plugin and would double-launch; Phase A is boot-once+serve+drain (what the exit criteria test). Hot restart needs a small additive `gateway`/supervisor re-bind path.

**✅ `rat` containerized (2026-06-02, branch `phase-1-adr-019-phase-b`, merged-from `phase-1`).** `core/Dockerfile` (multi-stage: static CGO-free binary → non-root alpine) + `core/cmd/rat/plane.container.yaml` (the demo plane baked at `/etc/rat/plane.yaml`) + `.dockerignore` + `make rat-image`. Proven: `podman run -p 7777:7777 rat/serve:dev` boots the daemon in-container (the stateplugin launches via local-process *inside* the container, non-root; gateway serves on `:7777`), `podman stop` (SIGTERM) drains cleanly. The ADR Phase-C *daemon-image* pulled forward; architecture unchanged.

**✅ `ratctl` — the first client connects to the orchestrator (2026-06-02, same branch).** A conversation with Tom reframed the active goal around **`rat` as an orchestrator service that many UIs connect to and drive** (CLI / VS Code / webapp), which is *orthogonal* to the data-dev plugin pipeline. Built `core/cmd/ratctl/` — a **separate** client binary (the kubectl to `rat serve`'s apiserver): `ratctl call <capability> --as <caller> --data <protojson>` resolves the capability generically, dials the gateway, issues the command (C1 envelope + C5 caller identity), prints the protojson response. `make ratctl-smoke` proves the client→orchestrator path end to end (authorized routes, undeclared = `PermissionDenied`). **Decision:** kept the declarative `rat serve --plane` model; **parked** Tom's runtime self-register idea in [`ideas/inbox.md`](../ideas/inbox.md) (scale feature; needs an ADR + the mutable-provider core change shared with the Phase-A reconciler-rewire gap).

**On the original Phase B (data-dev plugins through the gateway):** investigated + **deferred**. Two hard constraints found: (1) **F9** — the engine's Arrow result leg is in-proc-only (no network endpoint), so a *containerized* engine can't return query rows without the out-of-scope Flight leg or a shared-lake-in-proc read; (2) cross-container DuckLake sharing needs **Postgres + MinIO** (frozen `LaunchSpec` has no volume mount). Net: a fully-containerized data-dev pipeline is a heavy, podman-networking-fragile lift; recorded for when it's the priority. The orchestrator+clients track (above) is the current direction.

**Immediate next concrete step:** **ADR-019 Phase B** — write a `Dockerfile` per data-dev Python plugin (engine `duckdb-ml`, catalog `ducklake`, storage `minio-s3`, strategy `incremental-embed`), `CMD python main.py` honoring `RAT_PLUGIN_ADDR`, tagged `rat/<plugin>:dev`; a `data-dev-plane.yaml` (launch mode, `runtime: podman`, the I9 profile + each plugin's env); `rat serve --plane data-dev-plane.yaml` runs the real ML lakehouse mediated by the **real** core gateway (a `make data-dev-served` target). The data-dev-plane experiment is **complete + paused** (steps 2/3/4/6 done; 5 new plugins + gateway BFF; conformance 34/34, `make breaking` clean).

*(Optional parallel track: a synthesis writeup of the experiment's F1–F9 — what held / what bent — feeding any contract follow-ons.)*
- **Q02 — external peer review** (still owed per [reviews/09](../reviews/09-phase-1-gate-review.md) / [ADR-013](../docs/architecture/adrs/013-phase-1-spike-and-commitment-gate.md)) — *set aside as impractical for a solo dev (see the experiment above)*: get *outside human* eyes on the architecture before broad commitment. Freeze stays **local/unpushed** until then. ✅ **Kit fully prepped** (5 lens briefs + [outreach](../reviews/Q02-outreach-note.md) + [shortlist](../reviews/Q02-reviewer-shortlist.md) + [tracker](../reviews/Q02-tracker.md)). 🧪 **SIMULATED dry-run DONE (2026-06-02)** — a 5-agent deliberating panel (4 lenses + a defending maintainer; *AI personas*) ran the brief end-to-end → [`reviews/11-q02-*.md`](../reviews/) + [synthesis in the tracker](../reviews/Q02-tracker.md). Result: **0 hard / 3 soft freeze-reopen** (all additive, all catchable while local), enforcement spine *validated*, net read **GO / adjust-before-unfreeze**. **This does NOT discharge Q02** (AI ≠ outside eyes; weight like reviews/00–08). **Two human steps remain: (a) recruit the real reviewer(s)** (min architect + security — hand them the dry-run synthesis as a baseline to falsify), **and (b) land the pre-unfreeze punch-list** (see next bullet). Then → Q01.
- **🆕 Pre-unfreeze punch-list (the dry-run's #1 output)** — the additive items to resolve *before* the freeze is ever published, while changes are still cheap: **PU-1** a frozen conformance MUST that ArrowStream producers channel-authenticate the bytes leg (the ref trusts raw HTTP headers); **PU-2** give the keystone context-envelope/gateway-stamping its own two-reference conformance (it's the most-irreversible surface with the *weakest* coverage — qualifies ADR-015's "freeze validated"); **PU-3** attestation `expires_at` + revocation; **PU-4** decide v1 tenancy = isolation-only *or* add the cross-tenant delegation primitive now. ✅ **Drafted as [ADR-017](../docs/architecture/adrs/017-pre-unfreeze-contract-amendment-gate.md) (Proposed)** — the explicit gate: the freeze publishes only after (a) this punch-list resolves AND (b) the real Q02 runs. ✅ **PU-4 ratified 2026-06-02: isolation-only** (ADR-017 Q01 resolved — `DECISION_KIND_SHARING` advisory-not-enforced in v1; cross-tenant delegation is a future `v2` ADR). ✅ **Additive cut LANDED 2026-06-02** (`phase-1-q02-additive-cut`, 3 commits, all `make breaking`-clean + `core-test` green): **Cut 1** (PU-1 MUST, PU-4 isolation-only, 5a `min_revision`, 5b `Event` signing, PU-3 attestation lifecycle fields) + **enforcement demos** (PU-3 core expiry/revocation; PU-1 mTLS channel-auth conformance vector) + **5c** (storage vend read/write split). ✅ **PU-2 DONE 2026-06-02** — keystone context-carriage two-reference conformance (Go + Python clean-room impls cross-run 12 shared golden vectors; `make context-carriage` 12/12, both agree). ✅ **Rust storage SDK regen DONE** (ADR-018 connectionless rust closed it). 🎉 **The entire pre-unfreeze punch-list (PU-1/2/3/4 + 5a/5b/5c) is COMPLETE + verified, `make breaking` clean throughout.** The **sole remaining gate before the freeze leaves local/unpushed is the real Q02 external human review** — then re-seal `rat/2.x`. (Non-gating, in [backlog.md](backlog.md): the multi-tenant-availability cluster — AV-1 `core/lease` error-channel first — + ecosystem on-ramp EC-1.)
- **Phase 2+ are user-pull-gated** — phases.md **Gate B** (≥10 real solo users) must be met before building further. Phase 2 is NOT started.
- Residual follow-ons (non-blocking, in backlog): write-leg idempotency vs a real *idempotent format* ref (C1 residual); an explicit cloud metadata-egress drop + structured `IsolationAttestation` (D-series GA); core audit-record signing + hash chain (C4/C8 GA, seeded by D4's ed25519).
- ✅ **Codegen toolchain ([ADR-018](../docs/architecture/adrs/018-connectionless-codegen-local-plugins.md)) — COMPLETE.** SDK codegen moved from rate-limited remote BSR plugins to **local** plugins in pinned per-language images: **Go + TypeScript + Rust + Python all generate connectionless (fully BSR-free)**. Go/TS zero-churn; Rust's regen also **closed the 5c rust-storage gap**; Python uses a protoc-35 + grpcio-tools-1.80 hybrid (keeps protobuf 7.35.0 — no downgrade — **verified `make conformance` 32/32**). The ADR-017-cut rate-limit friction is gone.

CI (`make core-test` + `make core-test-podman` + `make breaking`) green.

## What's NOT in flight

- **Phase 2–5** — gated (Gate B: ≥10 solo users; Gate C/D). Not started.
- The broader **GTM / distribution** work (vision.md anti-goals) — Phase 4; the commitment cleared here is the *core build*, not the GTM motion.

## Branching (in force)

Work on `phase-1` (integration) or `phase-1-<slug>` (topic) sub-branches — **never commit to `main`** (a `PreToolUse` hook blocks it). Sub-branches merge back `--no-ff`. Full rules: [`.claude/rules/git-branching.md`](../.claude/rules/git-branching.md).

## Maintenance reminder

When this stream produces concrete output: update `done.md` → `current.md` → `phases.md` (if a phase moved) → `backlog.md` (if new work surfaced). Full rules in [CLAUDE.md](CLAUDE.md).
