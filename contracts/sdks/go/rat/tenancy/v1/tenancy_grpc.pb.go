// Tenancy axis service contract. Status: v1 (frozen — rat/1.2; ADR-003: control-plane = one ref + conformance).
//
// A `kind: tenancy` plugin decides tenant-boundary questions the core asks at
// decision points (a permission check, a sharing grant, a quota test).
//
// Capability mapping:
//   rat://tenancy/v1/decide -> Decide (a generic decision hook)
//
// C7 — TENANCY IS STRUCTURAL, NOT ADVISORY (reviews/00 Theme 4, reviews/01
// Finding 3): the tenant dimension lives in core primitives (RequestContext.identity.tenant
// threads every RPC; the state gateway namespaces by it; storage vends creds
// scoped to it). This plugin does NOT re-implement isolation — the core enforces
// it structurally. The plugin only answers POLICY questions the core can't
// hardcode (e.g. "may tenant A share dataset X with tenant B?"). The dangerous
// reading the review flagged — "isolation is an emergent property of 4 plugins
// agreeing" — is rejected: isolation is the core's job; this plugin is policy on
// top of an already-enforced boundary.
//
// v1 SCOPE — ISOLATION-ONLY (Q02 PU-4, ADR-017; ratified 2026-06-02): v1 tenancy is
// ISOLATION-ONLY. The core structurally enforces tenant ISOLATION (equality on
// RequestContext.identity.tenant across the state gateway, storage cred-vending, and
// the invoke gateway), and this plugin may DECIDE permission/quota questions the core
// then enforces. But DECISION_KIND_SHARING is ADVISORY-NOT-ENFORCED in v1: there is
// deliberately NO frozen primitive by which an *allowed* cross-tenant share is
// ACTIONED — no delegated-tenant / grant shape exists in the state or storage wire, so
// a plugin returning allowed=true for a SHARING decision computes a verdict the core
// CANNOT carry into a cross-tenant grant. Actioned cross-tenant sharing + hierarchical
// tenancy are a deferred `v2` (a delegation primitive in state/storage — its own ADR,
// only if a user pulls for it). Treat DECISION_KIND_SHARING as informational /
// policy-signalling in v1, not as an enforceable grant. (Q02 architect F5: "decidable
// but un-actionable"; the cheap, honest scope is to enforce only the isolation v1
// actually has.)
//
// ERROR MODEL (reviews/06 C-5 / freeze-blocker #9): same convention as
// identity.Authorize — transport/operational failures use gRPC status; a deny on
// a successful Decide uses the in-band `allowed` bool + enumerated `deny_code`;
// `reason` is LOG/AUDIT-ONLY and MUST NOT drive caller logic (enumeration oracle).

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: rat/tenancy/v1/tenancy.proto

package tenancyv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	TenancyService_Decide_FullMethodName = "/rat.tenancy.v1.TenancyService/Decide"
)

// TenancyServiceClient is the client API for TenancyService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type TenancyServiceClient interface {
	// rat://tenancy/v1/decide — answer a policy decision the core poses at a hook
	// point. The core enforces the verdict; the plugin only computes it.
	Decide(ctx context.Context, in *DecideRequest, opts ...grpc.CallOption) (*DecideResponse, error)
}

type tenancyServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewTenancyServiceClient(cc grpc.ClientConnInterface) TenancyServiceClient {
	return &tenancyServiceClient{cc}
}

func (c *tenancyServiceClient) Decide(ctx context.Context, in *DecideRequest, opts ...grpc.CallOption) (*DecideResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(DecideResponse)
	err := c.cc.Invoke(ctx, TenancyService_Decide_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// TenancyServiceServer is the server API for TenancyService service.
// All implementations must embed UnimplementedTenancyServiceServer
// for forward compatibility.
type TenancyServiceServer interface {
	// rat://tenancy/v1/decide — answer a policy decision the core poses at a hook
	// point. The core enforces the verdict; the plugin only computes it.
	Decide(context.Context, *DecideRequest) (*DecideResponse, error)
	mustEmbedUnimplementedTenancyServiceServer()
}

// UnimplementedTenancyServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedTenancyServiceServer struct{}

func (UnimplementedTenancyServiceServer) Decide(context.Context, *DecideRequest) (*DecideResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Decide not implemented")
}
func (UnimplementedTenancyServiceServer) mustEmbedUnimplementedTenancyServiceServer() {}
func (UnimplementedTenancyServiceServer) testEmbeddedByValue()                        {}

// UnsafeTenancyServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to TenancyServiceServer will
// result in compilation errors.
type UnsafeTenancyServiceServer interface {
	mustEmbedUnimplementedTenancyServiceServer()
}

func RegisterTenancyServiceServer(s grpc.ServiceRegistrar, srv TenancyServiceServer) {
	// If the following call panics, it indicates UnimplementedTenancyServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&TenancyService_ServiceDesc, srv)
}

func _TenancyService_Decide_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(DecideRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(TenancyServiceServer).Decide(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: TenancyService_Decide_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(TenancyServiceServer).Decide(ctx, req.(*DecideRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// TenancyService_ServiceDesc is the grpc.ServiceDesc for TenancyService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var TenancyService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "rat.tenancy.v1.TenancyService",
	HandlerType: (*TenancyServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Decide",
			Handler:    _TenancyService_Decide_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "rat/tenancy/v1/tenancy.proto",
}
