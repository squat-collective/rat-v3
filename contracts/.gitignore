# Generated SDKs live in sdks/<lang>/ and ARE committed (ADR-006 D1) — regenerate
# via `make gen-sdks`, never hand-edit. Nothing proto-related is ignored here.

# Go SDK build/test scratch (deps are resolved on build, not vendored here).
sdks/go/go.sum
